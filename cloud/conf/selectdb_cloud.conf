# // meta_service
brpc_listen_port = 5000
brpc_num_threads = -1
fdb_cluster = xxx:yyy@127.0.0.1:4500
fdb_cluster_file_path = ./conf/fdb.cluster
http_token = greedisgood9999

# // doris txn config
stream_load_default_timeout_second = 600
stream_load_default_precommit_timeout_second = 3600
label_keep_max_second = 259200
expired_txn_scan_key_nums = 1000

# // logging
log_dir = ./log/
# info warn error
log_level = info
log_size_mb = 1024
log_filenum_quota = 10
log_immediate_flush = false
# log_verbose_modules = *

# // recycler config
recycl_interval_seconds = 3600
index_retention_seconds = 172800
partition_retention_seconds = 86400
rowset_retention_seconds = 10800
recycle_concurrency = 16
recycle_standalone_mode = false
